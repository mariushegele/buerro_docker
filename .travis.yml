
services:
    - docker

before_install:
    - git submodule update --init --remote --recursive
    - docker build -t buerro .

script:
    - docker exec -it buerro make test

git:
    submodules: false
